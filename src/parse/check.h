/**
 * 
*/
#ifndef __CHECK_H__
#define __CHECK_H__

#include <stdint.h>
#include <stdbool.h>
#include <string.h>

// #include "parse/term_stack.h"


// inline bool check_bool_sort(term_stack_t *stack, uint32_t elem) {
//     return stack->data[elem].tag == TAG_SORT && 
//            !strcmp(stack->data[elem].value.ident.symbol, "Bool");
// }

// bool check_real_sort(term_stack_t *stack, uint32_t elem);
// bool check_bitvec_sort(term_stack_t *stack, uint32_t elem);
// bool check_array_sort(term_stack_t *stack, uint32_t elem);


#endif